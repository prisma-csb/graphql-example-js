endpoint: __YOUR_PRISMA_ENDOINT__ # This project uses a hosted Demo server with disabled mutations
datamodel: datamodel.prisma

generate:
  - generator: javascript-client
    output: ../src/generated/prisma-client/

hooks:
  post-deploy:
    - prisma generate

seed:
  run: node prisma/seed.js